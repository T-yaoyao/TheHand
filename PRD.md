# PRD: SuperMan — 端到端交付全栈项目的"超级个体"

## 一、产品定位

### 一句话定义

> PM 在聊天框里用自然语言描述需求，系统自动完成澄清、拆解、代码生成、验证、提 PR。开发者只管 review。

### 核心价值

Claude Code 是**开发者的工具**——开发者自己想清楚要改什么，告诉 AI 怎么改。
SuperMan 是 **PM 的流水线**——PM 说一句话，系统自动完成全部工作。

| | Claude Code | SuperMan |
|---|---|---|
| 驱动者 | 开发者 | PM |
| 开发者需要做什么 | 想清楚需求，告诉 AI 怎么改 | 等 PR，review 就行 |
| PM 需要做什么 | 不会用 | 直接对话 |

**核心区别：Claude Code 让开发者更快，SuperMan 让 PM 直接能交付。**

### 系统是什么

一个 Web 应用，三部分组成：

```
PM 打开网页 → 在聊天框输入需求 → 系统自动分析、生成代码、提 PR
```

系统本身是**项目无关的通用平台**，Conduit 只是第一个"接入项目"。架构上是可插拔的，未来换一个项目（比如 Vue + Go 的仓库），只需新增项目配置，核心逻辑不动。

### 系统不是什么

- 不是 Conduit 专属工具——Conduit 是测试对象，系统本身是通用的
- 不是一个聊天机器人——不是闲聊，是有明确流程的状态机
- 不是一个自动部署系统——只到 PR + 验证通过为止，不负责部署
- 不是套壳 ChatGPT——只做了一个对话窗口贴模型输出，未接入仓库与测试链路

---

## 二、核心用户角色

| 角色 | 诉求 | 使用方式 |
|------|------|---------|
| PM | 用自然语言描述需求，看到结构化方案，审批/修改后一键生成代码 | Web 对话页 |
| 开发 Lead | 审查 AI 生成的 PR，管理 Skill 注册，控制流程 | GitHub PR Review |
| 系统（AI） | 澄清需求、拆解方案、定位模块、生成代码、跑测试 | 自动执行 |

---

## 三、核心链路

PM 输入一句话，系统自动完成：

```
1. 澄清：识别模糊需求中的歧义，主动追问（最多 3 轮）
2. PM 回复后，系统输出结构化方案（改哪些文件、加什么代码）
3. PM 审批方案（可修改、可终止）
4. 系统在目标仓库里生成代码
5. 自动跑 lint + 单测 + 集成测试
6. 验证通过后创建 PR
```

每一步 PM 都可以介入修改。

### 澄清 Agent 状态机

```
idle → clarifying → clarified → (进入方案阶段)
                   → failed → (降级处理)
```

- **idle**：等待 PM 输入
- **clarifying**：正在与 PM 对话澄清需求（最多 3 轮）
- **clarified**：需求已结构化，进入方案阶段
- **failed**：超过 3 轮仍未澄清

**降级策略（failed 时）：**
1. 系统使用已有信息 + 合理默认值，生成"最可能的"结构化需求
2. 在方案中标注"以下信息为系统推测，请确认"
3. PM 可以修改后继续，或终止流程

**上下文传递策略：**

每轮澄清传给 LLM 的内容：
```
当前结构化需求 JSON（中间状态）+ PM 的新回复
```

- **不传全量对话历史**——避免 token 暴涨
- **结构化需求 JSON 作为唯一事实来源**——随每轮澄清逐步补全
- 每轮 LLM 的任务：根据 PM 新回复更新 JSON，如果信息仍不完整则继续追问

**示例流转：**
```
第 1 轮：PM "给文章加阅读时长"
→ LLM 输出：{ type: "add_field", entity: "Article", ...缺失字段... }
→ 追问：估算还是精确？显示在哪里？

第 2 轮：PM "估算的，都显示"
→ 输入给 LLM：{ 上轮 JSON } + "估算的，都显示"
→ LLM 输出：{ 完整 JSON，所有字段已补全 }
→ 状态：clarified
```

---

## 四、技术架构

```
┌─────────────────────────────────────────────┐
│              前端 (React)                    │
│  对话页 │ 需求状态流 │ 方案审批 │ PR 预览    │
│  (PM 不碰代码，只看流程状态和方案)             │
└──────────────────┬──────────────────────────┘
                   │ REST API / SSE
┌──────────────────▼──────────────────────────┐
│              Node.js 后端                    │
│  会话管理 │ 流程状态机 │ Git 操作 │ 事件存储  │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│       AI 编排层 (核心，项目无关)               │
│                                              │
│  Orchestrator (状态机调度)                    │
│    ├─ 澄清 Agent → 结构化需求 JSON            │
│    ├─ 方案 Agent → 改哪些文件、加什么          │
│    ├─ 编码 Agent → 生成代码                   │
│    │    ├─ Skill: add-field                  │
│    │    ├─ Skill: add-page                   │
│    │    └─ Skill: ...                        │
│    └─ 测试 Agent → lint + 单测 + 集成测试     │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────▼──────────────────────────┐
│         项目适配层 (可插拔)                    │
│  projects/conduit/                           │
│    ├─ project.json (元信息、技术栈、测试命令)  │
│    ├─ skills/ (项目专属 Skill)                │
│    └─ context/ (模块、路由、模型定义)          │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────▼──────────┐
        │   Conduit 仓库 (Git) │
        │  sandbox-repo / fork │
        └─────────────────────┘
```

### 数据模型定义

**Requirement（需求）**
```json
{
  "id": "string",
  "status": "pending | clarifying | clarified | generating | testing | done | failed",
  "pmInput": "string",
  "structuredRequirement": {},
  "createdAt": "timestamp",
  "updatedAt": "timestamp"
}
```

**Conversation（对话）** — 与 Requirement 为 1:N 关系
```json
{
  "id": "string",
  "requirementId": "string",
  "role": "pm | system",
  "content": "string",
  "round": "number",
  "createdAt": "timestamp"
}
```

**Execution（执行记录）** — 可观测性必需
```json
{
  "id": "string",
  "requirementId": "string",
  "agent": "clarification | plan | coding | testing",
  "skill": "string | null",
  "input": {},
  "output": {},
  "status": "success | failed",
  "reason": "string | null",
  "inputTokens": "number",
  "outputTokens": "number",
  "latencyMs": "number",
  "estimatedCost": "number",
  "createdAt": "timestamp"
}
```

---

## 五、产出结构

详见 [技术路线.md](技术路线.md) 第一章"目录结构"。

---

## 六、PM 端到端交互流程

```
┌─────────────────────────────────────────────────────────────┐
│ 1. PM 输入需求                                                │
│    [输入框] "给文章加个阅读时长"                                │
├─────────────────────────────────────────────────────────────┤
│ 2. 系统追问（澄清阶段，最多 3 轮）                              │
│    [系统] 需要确认几个问题：                                    │
│    ① 阅读时长是估算还是精确计时？                               │
│    ② 显示在哪些页面？                                         │
│    [输入框] PM 回复                                            │
│    [跳过，使用默认值] ← PM 可选择跳过                           │
├─────────────────────────────────────────────────────────────┤
│ 3. 方案审批                                                   │
│    [系统] 以下是技术方案：                                      │
│    ┌─────────────────────────────────┐                       │
│    │ 修改文件：                       │                       │
│    │ - server/models/Article.js     │                       │
│    │ - server/routes/api/articles.js│                       │
│    │ - client/src/components/...    │                       │
│    │                                │                       │
│    │ 变更内容：                      │                       │
│    │ - 新增 readTime 字段           │                       │
│    │ - API 返回该字段               │                       │
│    │ - 前端展示阅读时长             │                       │
│    └─────────────────────────────────┘                       │
│    [确认] [修改方案] [终止] ← PM 可介入                        │
├─────────────────────────────────────────────────────────────┤
│ 4. 执行中                                                     │
│    [系统] 代码生成中... ████████░░ 80%                        │
│    [系统] 测试通过 ✅                                          │
│    [系统] 测试失败 ❌ [查看详情] [重试] [终止]                  │
├─────────────────────────────────────────────────────────────┤
│ 5. 完成                                                       │
│    [系统] PR 已创建：#123                                     │
│    [查看 PR] [查看变更 diff] [新需求]                          │
└─────────────────────────────────────────────────────────────┘
```

PM 在每个环节都可以介入：
- **澄清阶段**：跳过追问，使用默认值
- **方案审批**：修改方案、终止流程
- **执行中**：查看详情、重试、终止
- **完成后**：查看 PR、查看 diff、开始新需求

---

## 七、MVP 交付标准（P0）

### 7.1 范围

**只做一条完整链路："给 Article 模型新增一个字段"**

理由：
- 这是 Conduit 最典型的变更模式（加字段 → 改 model → 改 API → 改前端展示）
- 能覆盖前后端 + 数据库三层
- 涉及的 Skill 足够有代表性
- 复杂度可控

### 7.2 MVP 优先级（按关键程度排序）

| 优先级 | 模块 | 原因 |
|--------|------|------|
| P0-1 | **澄清 Agent** | 最核心、最难，决定系统能否用 |
| P0-2 | **模块定位** | 知道改哪些文件，代码生成的前提 |
| P0-3 | **代码生成** | 调 LLM API，生成修改后的文件 |
| P0-4 | **测试 + 提 PR** | 验证正确性，相对标准 |
| P0-5 | **Web 界面** | 最后做，MVP 阶段可先用 CLI |

### 7.3 各阶段输入输出

| 阶段 | 输入 | 输出格式 |
|------|------|---------|
| 澄清 | PM 的模糊需求 | 结构化需求 JSON |
| 方案 | 结构化需求 | `[{ path, changeDescription }]` |
| 代码生成 | 方案输出 | `[{ path, content, summary }]` |
| 测试 | 代码生成输出 | 测试结果（pass/fail + 详情） |
| 提测 | 测试结果 | PR 链接 |

### 7.4 通过标准

- 端到端跑通 1 条需求链路
- 代码写入 Conduit 实仓（sandbox-repo 基于 Conduit fork）
- 三端齐备：前端对话页 + Node 后端 + AI 编排层均有真实实现
- **三级验证全部通过**（见 7.5）
- 成功创建 PR

### 7.5 验证体系

| 验证层级 | 能验证什么 | MVP 是否需要 |
|----------|-----------|-------------|
| Lint | 语法正确、风格一致 | 必须 |
| 单测 | 函数逻辑正确 | 必须 |
| 集成测试 | 启动应用，调 API 确认返回值正确 | 必须 |
| 冒烟测试 | 跑完整用户路径 | P1 加分项 |
| 人工 Review | 逻辑合理性 | PR 环节 |

---

## 八、代码写入机制

### 流程

```
PM 输入 → 澄清 → 方案 → 模块定位
    │
    ▼
[清洁检查] 检测 sandbox-repo 是否有脏状态
    │  有脏状态 → 自动 git checkout -- . 清理
    │  清理失败 → 拒绝执行，提示 PM
    ▼
确定要改哪些文件
    │
    ▼
AI 生成每个文件的修改内容（整体重写方式）
    │
    ▼
fs.writeFileSync 写入磁盘
    │
    ▼
[Diff 检查] 对比重写前后文件，检测是否引入无关变更
    │  有无关变更 → 告警，等待 PM 确认
    ▼
跑 lint + 测试
    │  失败 → 自动 git checkout -- . 回滚 → 报错给 PM
    │  成功 ↓
    ▼
git add → git commit → 创建 PR
```

### 回滚机制

| 场景 | 处理方式 |
|------|---------|
| lint/测试失败 | 自动 `git checkout -- .` 回滚到写入前状态 |
| 写入过程中断 | 清洁检查阶段自动检测并 reset |
| PM 取消操作 | 回滚到最近一次干净状态 |

### 并发约束

**MVP 阶段为单用户串行执行**，不做并发处理。同一时间只允许一个需求在执行流程中。

---

## 九、完整示例

PM 说："给文章加阅读时长"

```
步骤 1: 读取相关文件
  → 读 server/models/Article.js
  → 读 server/routes/api/articles.js
  → 读 client/src/components/ArticlePreview.js

步骤 2: AI 生成修改后的完整文件
  → 输出 Article.js (加了 readTime 字段)
  → 输出 articles.js (API 返回 readTime)
  → 输出 ArticlePreview.js (显示阅读时长)

步骤 3: 写入磁盘
  → fs.writeFileSync('sandbox-repo/server/models/Article.js', ...)
  → fs.writeFileSync('sandbox-repo/server/routes/api/articles.js', ...)
  → fs.writeFileSync('sandbox-repo/client/src/components/ArticlePreview.js', ...)

步骤 4: 验证
  → cd sandbox-repo && npm run lint
  → cd sandbox-repo && npm test
  → 启动应用，调 API 确认 readTime 字段返回正确（集成测试）

步骤 5: 提交
  → git add -A
  → git commit -m "feat: add readTime to Article"
  → 创建 PR
```

---

## 十、团队分工

2 人团队，按 MVP 优先级分工：

| 角色 | 负责 | 工作量评估 |
|------|------|-----------|
| A | 澄清 Agent + 方案 Agent + 编码 Agent + Skill 体系（核心 AI 链路） | 重（4 个 Agent + Skill） |
| B | 后端 API + 流程状态机 + Git 操作 + 前端对话页（工程化支撑） | 中（标准 CRUD + 前端） |

### 接口边界与对接计划

| 时间点 | A 交付物 | B 交付物 | 对接内容 |
|--------|---------|---------|---------|
| 第 1 周末 | CLI 版澄清→代码生成链路跑通 | 后端 API 骨架 + 前端对话页骨架 | A 的 CLI 输出对接 B 的 API |
| 第 2 周中 | Skill 体系 + 测试 Agent | 流程状态机 + Git 操作 | 全链路联调 |
| 第 2 周末 | 修复问题 + 优化 | 前端完善 + 集成测试 | 端到端验收 |

---

## 十一、P1 加分项

| 优先级 | 加分项 | 实现难度 | 对应赛题要求 |
|--------|--------|---------|-------------|
| P1-1 | 抽象到位（Skill 注册制） | 中 | 新增需求模式仅需新增 1 个 Skill 文件 |
| P1-2 | 澄清深度（歧义识别+主动追问） | 中 | 识别 PM 模糊需求中的歧义并主动追问 |
| P1-3 | 断点重放 | 中 | 流程任一阶段可暂停/修改/重放下游 |
| P1-4 | 跨栈一致性 | 高 | 后端字段变更自动驱动前端类型/Mock/调用点更新 |
| P1-5 | 业务上下文反哺 | 高 | 历史需求结构化沉淀，相似新需求自动召回旧方案 |
| P1-6 | 可观测性 | 中 | 每次 AI 调用记录 tokens/延迟/成本，附简易监控面板 |

---

## 十二、技术约束

- **主模型**：doubao-seed-2.0 lite（火山方舟），20w 上下文，API 调用
- **仓库**：Conduit（https://github.com/TonyMckes/conduit-realworld-example-app）
- **技术栈**：React 18 + Vite + Express 4 + Sequelize + PostgreSQL，纯 JavaScript
- **如需额外模型须自付费并在 README 声明**
- **EP / API key 仅限本课题挑战开发使用，严禁外泄**

### 模型能力验证（前置步骤）

**在正式开发前，必须先验证模型能力。**

验证步骤：
1. 手动用 doubao-seed-2.0 lite 测试"模糊需求 → 结构化 JSON"的翻译能力
2. 手动测试"给定文件上下文 + 需求 → 生成修改后代码"的能力
3. 评估生成的代码能否通过 lint + 测试

降级方案：
- 如果模型在某环节能力不足，将该环节拆分为更小的 prompt 子任务
- 如果拆分后仍不足，考虑在该环节使用更强模型（需自付费并在 README 声明）

### Prompt 版本管理

- prompt 模板存放在 `core/prompts/` 目录下
- 每个 prompt 有版本号，修改时递增
- 通过配置文件指定使用哪个版本的 prompt

### 安全基本要求

| 风险 | MVP 防护措施 |
|------|-------------|
| Prompt 注入 | 对 PM 输入做基础过滤（去除系统指令关键词） |
| 代码注入 | 生成的代码写入前做基础检查（禁止 `exec`、`eval`、`rm -rf` 等危险操作） |
| API 密钥 | 环境变量管理，不硬编码 |
| Git 权限 | MVP 阶段仅操作本地 sandbox-repo |

### Git 认证与 PR 创建降级策略

| 场景 | 降级方式 |
|------|---------|
| GITHUB_TOKEN 缺失 | 提示 PM 配置 token，同时提供手动 git push 命令 |
| 网络不通 | 提示网络问题，提供手动 git push + gh pr create 命令 |
| API 限流 | 提示稍后重试，提供手动命令 |
| 其他错误 | 展示友好错误信息 + 完整的手动操作步骤 |

---

## 十三、可扩展性设计

### 核心原则

换一个项目接入，**核心逻辑零改动**，只需新增项目配置。

### 接入新项目的工作量

| 工作项 | 内容 | 预估时间 |
|--------|------|---------|
| project.json | 描述项目元信息（技术栈、目录结构、测试命令） | 30 分钟 |
| context/ | 梳理项目的模块、路由、模型定义 | 几小时 |
| skills/ | 已有 Skill 无需改动；新需求模式才需新增 | 视情况 |

### Skill 接口契约

```js
{
  name: 'add-field',
  description: '给模型新增字段',
  canHandle: (requirement) => requirement.type === 'add_field',
  execute: async (requirement, projectContext) => {
    return [
      {
        path: 'server/models/Article.js',
        content: '...',
        summary: '新增 readTime 字段，类型 INTEGER'
      }
    ]
  }
}
```

### 练手题覆盖

MVP 需覆盖以下练手题类型：

**L1 入门（纯前端/单模块）：**
- 文章列表加阅读量字段
- Popular Tags 侧边栏前 5 个打标
- 个人主页新增 About Me Tab
- 文章详情页新增字数统计

**L2 进阶（跨栈一致性）：**
- 文章加封面图字段
- 评论支持点赞（含幂等）
- 文章草稿功能
- 文章最后编辑时间展示

**L3 挑战（模糊需求澄清）：**
- 面对表达不完整的需求，能否主动追问
- 多模块协同拆解能力
- 反模式识别能力
