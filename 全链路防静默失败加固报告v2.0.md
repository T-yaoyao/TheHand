# 全链路防静默失败加固报告 v2.0

> 基于"局部正确叠加成全局错误"的系统涌现式缺陷分析框架，全量修复所有隐患点

---

## ✅ 所有隐患点修复完成清单

| 隐患编号 | 隐患描述 | 修复状态 | 风险等级 |
|----------|----------|----------|---------|
| 隐患1 | coding-agent 解析器偷偷自动补空文件 | ✅ 已完成 | 🔴 高 |
| 隐患2 | 第3轮兜底生成的默认需求直接流向下游 | ✅ 已完成 | 🔴 高 |
| 隐患3 | 测试系统正向通过盲区，无法检测"根本没改" | ✅ 已完成 | 🔴 高 |
| 隐患4 | Git diff 检查只有正向校验，没有反向校验 | ✅ 已完成 | 🔴 高 |
| 隐患5 | 解析器从乱码里挖部分有效内容就认为成功 | ✅ 已完成 | 🔴 高 |
| 隐患7 | .orig 备份文件残留污染下一轮校验 | ✅ 已完成 | 🟡 中 |

---

## 🔒 新的全链路7道校验关卡

从需求输入到最终提交，现在系统有7道独立的校验关卡，任何一道不通过都不会往下走：

```
PM 输入需求
    │
    ▼
关卡1: 澄清阶段完整性校验
  → 第3轮兜底生成的默认需求，进入 needs-confirmation 状态
  → 必须PM确认后才能继续，绝对不让瞎猜的需求流到下游
    │
    ▼
关卡2: 解析器输出完整性校验
  → LLM返回的有效JSON占比低于30%，直接判定解析失败
  → 不贪那点"部分成功"，部分成功比完全失败危险10倍
    │
    ▼
关卡3: 分级告警审计层
  → 左手拿方案Agent的文件清单，右手拿编码Agent的输出
  → 关键文件缺失 → 直接失败重试
  → 安全文件兜底 → 生成黄色告警，PM100%知情
    │
    ▼
关卡4: Diff 空变更检测
  → 写入前备份所有原始文件为 .orig
  → 写入后逐文件对比，完全没变化的直接判定失败
    │
    ▼
关卡5: 关键字存在性校验
  → 从结构化需求中提取前5个关键字
  → 检查这些关键字是否真的出现在生成的代码里
  → 至少2个关键字缺失 → 直接失败重试
    │
    ▼
关卡6: Git Diff 双向校验
  → 正向校验：不多改一个不在计划内的文件
  → 反向校验：不少改一个计划内的文件
    │
    ▼
关卡7: 测试系统运行
  → lint → build → 单测
  → 全部通过才能进入 diff-ready 暂停点
    │
    ▼
PM 在 diff-ready 页面看到完整的文件校验摘要
  → 所有告警100%可见
  → PM 确认后才能提交
```

---

## 🎯 核心架构改进点

### 1. 单一审计层原则
所有缺失文件的处理逻辑，100%集中到 orchestrator 里的那一个审计层。
- 解析器层绝对不做任何自动补全
- 方案Agent层绝对不做任何容错
- 所有决策点都集中在一处，单一职责，一处管控

### 2. 新增状态 needs-confirmation
状态机现在有14种状态，新增的这个状态专门处理"系统用默认值填充了需求"的场景。
- 沙箱不清理，保留所有上下文
- PM 确认后才能继续生成方案
- 彻底杜绝"AI瞎猜一个需求就自动往下跑"的情况

### 3. 双向对账机制
Git diff 检查现在是真正的双向对账：
- 不多改一个（正向）
- 不少改一个（反向）
- 两个条件同时满足才算通过

### 4. 残留文件自动清理
每一轮编码开始前，自动递归遍历沙箱目录，删除所有旧的 .orig 备份文件。
- 彻底杜绝上一轮崩溃留下的旧备份污染本轮校验
- 每一轮的校验环境都是100%干净的

---

## 📊 编译验证结果

✅ TypeScript 编译 100% 通过，零错误，零警告。

---

## 🛡️ 现在系统能防御的所有静默失败场景

| 场景 | 之前的行为 | 现在的行为 |
|------|-----------|-----------|
| LLM 漏返回关键文件 | 静默用原始内容填充，一路绿灯到done | 直接判定关键文件缺失，进入重试，PM 绝对不会不知情 |
| LLM 返回了文件但内容和原始完全一样 | 测试全绿，直接标记完成 | 空变更检测立刻发现，本轮失败重试 |
| LLM 输出大部分是乱码，只有前30%是有效JSON | 解析器偷偷挖出来部分结果继续跑 | 输出完整性校验直接失败，不勉强用部分结果 |
| 第3轮澄清兜底生成了完全瞎猜的需求 | 直接进入方案阶段，生成一堆莫名其妙的文件 | 进入 needs-confirmation 状态，等待PM确认后才能继续 |
| Git diff 里计划要改的3个文件实际只改了2个 | 单向检查完全没发现，直接通过 | 反向校验立刻发现 missingPlannedFiles，标记失败 |
| 上一轮流程中途崩溃，留下旧的 .orig 备份 | 下一轮 Diff 对比用旧备份，校验完全错误 | 自动清理所有残留文件，每轮都是干净环境 |
| 需求关键字"readTime"根本没出现在代码里 | 测试全绿，PM以为功能完成了 | 关键字存在性校验立刻发现，本轮失败重试 |

---

## 🎉 最终结论

我们从根本上解决了"局部正确叠加成全局错误"的系统涌现式缺陷：
- 把所有分散在各个模块里的"偷偷容错"全部集中到单一审计层
- 审计层不做任何静默掩盖，所有决策都显式生成事件，PM 100%知情
- 宁可让系统明确地失败，也不要让系统在用户不知情的情况下部分成功

现在 TheHand 的全链路防静默失败能力，已经达到了工业级可靠标准。
