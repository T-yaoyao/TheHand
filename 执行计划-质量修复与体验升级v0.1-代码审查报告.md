# TheHand 代码审查报告 v0.1

> 审查日期：2026-05-31
> 审查范围：core / backend / frontend 全部源码
> 审查视角：用户信任、代码质量、竞品差距

---

## 一、P0 信任修复验证

### P0-1：编排器 commit/apply 失败后标记 done ✅ 已修复

**原始问题**：orchestrator.ts:408-411 commit 失败仍标记 `done`，420-422 apply 失败被静默吞掉。

**当前状态**：已重构为三阶段架构 `run()` → `phaseCoding()` → `phaseCommit()`，所有失败点均标记 `failed` 并提供 `userMessage`。

### P0-2：编排器 plan 阶段暂停 + 审批端点 ✅ 已修复

**三阶段结构** — [orchestrator.ts](core/src/orchestrator/orchestrator.ts)
- `run()` → `phaseCoding()` → `phaseCommit()` 三层拆分 ✅
- plan-ready 后 `yield plan-ready` + `return` 暂停 ✅
- 检测 `plan-approved` 跳转 `phaseCoding()` ✅
- 检测 `diff-ready` 跳转 `phaseCommit()` ✅

**4 个端点** — [requirements.ts](backend/src/routes/requirements.ts)
- `approve-plan` line 234 ✅
- `reject-plan` line 264 ✅
- `commit` line 294 ✅
- `rollback` line 324 ✅

**runningJobs 释放** — [orchestrator-runner.ts:117-123](backend/src/orchestrator-runner.ts#L117-L123)
- `plan-ready` 和 `diff-ready` 时立即 `runningJobs.delete()` ✅

**前端 api.ts** — [api.ts:107-129](frontend/src/services/api.ts#L107-L129)
- `approvePlan` / `rejectPlan` / `commitChanges` / `rollbackChanges` 四个方法齐全 ✅

**⚠️ 小问题 1**：`approve-plan` 的 `runOrchestratorForRequirement` 是 fire-and-forget（无 await）。如果 orchestrator 启动时就抛异常（如 API key 未配置），客户端始终收到 `{ ok: true }`。当前 getDeps 有缓存，启动阶段基本不会失败，**可接受**。

**⚠️ 小问题 2**：`rollback` 端点只改状态不清理沙箱文件。但用户撤回后一定会重新触发编排器，`sandboxManager.create` 每次新建沙箱，**实际不影响**。

### P0-3：DELETE 需求清理关联数据 ✅ 已修复

[requirements.ts:162-174](backend/src/routes/requirements.ts#L162-L174)：已清理 `conversations`、`executions`、`change_history`、`lessons` 四张表。

---

## 二、P1 体验加固验证

### P1-3：tokenHistory 裁剪 ✅

[llm-client.ts:133-135](core/src/llm/llm-client.ts#L133-L135)：
```typescript
if (this.tokenHistory.length > 1000) {
  this.tokenHistory = this.tokenHistory.slice(-500)
}
```
阈值 1000 → 裁剪到 500，逻辑正确。

### P1-4：SSE events 裁剪 ✅

[useSSE.ts:58-61](frontend/src/hooks/useSSE.ts#L58-L61)：
```typescript
setEvents((prev) => {
  const next = [...prev, event]
  return next.length > 500 ? next.slice(-250) : next
})
```
阈值 500 → 裁剪到 250，逻辑正确。

### P1-5：toast timer 管理 ✅（纠正误判）

[App.tsx:35-48](frontend/src/App.tsx#L35-L48) 使用 `useRef` 管理 timer，`useEffect` cleanup 清理。搜遍整个代码库没有 `let timer` 声明，不存在变量遮蔽问题。**之前的 report.ts 判断是误判，撤回。**

### P1-1（新增发现）：orchestrator-runner.ts:135 catch 块无条件覆盖状态 ❌ 未修复

**问题**：顶层 catch 无条件写入 `status = 'failed'`，即使编排器内部已正确标记 `done`。

```typescript
} catch (e: unknown) {
  execute(`UPDATE requirements SET status = 'failed', ...`)  // 无条件覆盖
}
```

**场景**：编排器完成所有阶段标记 `done`，最后清理沙箱时抛异常，catch 把状态改回 `failed`。

**修复方案**：
```typescript
} catch (e: unknown) {
  const message = e instanceof Error ? e.message : String(e)
  const current = queryOne('SELECT status FROM requirements WHERE id = ?', [requirementId]) as { status: string } | undefined
  if (current && current.status !== 'done' && current.status !== 'failed') {
    execute(`UPDATE requirements SET status = 'failed', updated_at = datetime('now') WHERE id = ?`, [requirementId])
  }
  pushEvent(requirementId, {
    type: 'failed',
    requirement: { id: requirementId, status: current?.status === 'done' ? 'done' : 'failed' },
    error: message,
  })
}
```

---

## 三、P2 竞品差距补齐验证

### P2-1：Diff 预览 ✅

- 编排器测试通过后执行 `git diff` 获取 diff 内容 ✅
- `requirement.status = 'diff-ready'` 暂停 ✅
- yield `diff-ready` 事件附带 diff 字符串和文件列表 ✅
- 前端新增 `'diff'` Tab，展示 diff + 文件列表 + 确认提交/撤回按钮 ✅
- StatusFlow PIPELINE 新增 `{ keys: ['diff-ready'], label: '确认' }` ✅
- types.ts OrchestratorEvent 新增 `diff-ready` 类型 ✅

### P2-2：错误友好化 ✅

| 失败位置 | userMessage | 文件 |
|---------|------------|------|
| 方案 3 轮失败 | "方案生成失败，请尝试重新描述需求…" | orchestrator.ts:212 |
| 编码无有效文件 | "代码生成失败，请检查需求描述…" | orchestrator.ts:318 |
| 测试 3 轮失败 | "代码测试未通过（已重试 3 次）…" | orchestrator.ts:391 |
| commit 失败 | "代码提交失败，可能是沙箱 Git 配置问题…" | orchestrator.ts:479 |
| apply 失败 | "代码变更未能应用到源仓库…" | orchestrator.ts:493 |

StatusFlow 优先展示 `userMessage`，可展开查看 `error` 技术详情。

types.ts:239 `failed` 事件新增 `userMessage?: string` 字段 ✅

### P2-3：VALID_STATUSES ✅

[requirements.ts:16](backend/src/routes/requirements.ts#L16)：
```typescript
const VALID_STATUSES = ['idle', 'clarifying', 'clarified', 'waiting-for-pm', 'planning',
  'plan-approved', 'plan-rejected', 'coding', 'testing', 'diff-ready', 'done', 'failed', 'reverted']
```

[status.ts](frontend/src/utils/status.ts) 的 `STATUS_LABELS` 和 `STATUS_COLORS` 也覆盖了 `reverted` 和 `diff-ready`。

### P2-4：handleResponse 兼容 204 ✅

[api.ts:28-35](frontend/src/services/api.ts#L28-L35)：
```typescript
if (res.status === 204) return {} as T
```

---

## 四、新发现待修复项

### 4.1 clarification-agent.ts:93 贪婪正则 🔴 高优先级

**文件**：[clarification-agent.ts:93](core/src/agents/clarification-agent.ts#L93)

**问题**：
```typescript
const match = response.match(/\{[\s\S]*\}/)  // 贪婪：匹配最远的 }
```

LLM 输出包含多个 JSON 块时（先解释再给 JSON），贪婪匹配从第一个 `{` 匹配到最后一个 `}`，中间所有内容都被吞进去，`JSON.parse` 必然失败。

agent-runner.ts:188 已经用的是非贪婪 `\{[\s\S]*?\}`，但 clarification-agent.ts 没改。

**修复**：`/\{[\s\S]*?\}/`（加一个 `?` 变非贪婪）

**影响**：JSON 解析失败 → 追问失败 → 澄清阶段降级 → 垃圾需求进入下游

---

### 4.2 agent-runner.ts:86-91 ToolContext 是空壳 🟡 中优先级

**文件**：[agent-runner.ts:86-91](core/src/agents/agent-runner.ts#L86-L91)

**问题**：
```typescript
const result = await tool.call(parsed.data, {
  sandboxPath: '',              // ← 空字符串
  backupStore: {
    save: async () => {},       // ← 空操作
    restore: async () => null,
  },
})
```

- `sandboxPath` 传空字符串 → file-write-tool 的路径校验形同虚设
- `backupStore` 是空操作 → 写入失败时无法回滚

**修复**：AgentRunner 构造时接收 sandboxPath，run() 时传入真实 context。

**影响**：路径检查失效，写入失败不可恢复

---

### 4.3 clarification-agent.ts:152-157 fallback 产生垃圾需求 🟡 中优先级

**文件**：[clarification-agent.ts:152-157](core/src/agents/clarification-agent.ts#L152-L157)

**问题**：
```typescript
function currentOrDefault(text: string): StructuredRequirement {
  return {
    type: 'unknown',      // ← 不匹配任何 Skill
    entity: 'unknown',    // ← plan agent 无法定位文件
    scope: 'fullstack',
    description: text.slice(0, 200),
  }
}
```

`type: 'unknown'` 不匹配任何 Skill，plan agent 也很难生成有效方案。降级等于放弃。

**修复**：fallback 时至少尝试从文本推断 type：
```typescript
function currentOrDefault(text: string): StructuredRequirement {
  let type = 'unknown'
  if (/删除|remove|delete/i.test(text)) type = 'delete_field'
  else if (/加|新增|添加|add/i.test(text)) type = 'add_field'
  else if (/改|修改|update|modify/i.test(text)) type = 'modify_api'
  return { type, entity: 'unknown', scope: 'fullstack', description: text.slice(0, 200), isDefaulted: true }
}
```

**影响**：降级后下游全废

---

### 4.4 skill-registry.ts:216 buildCanHandle 精确匹配 🟡 中优先级

**文件**：[skill-registry.ts:216](core/src/skill-registry/skill-registry.ts#L216)

**问题**：
```typescript
const match = expression.match(/requirement\.type\s*===\s*['"](\w+)['"]/)
if (match) { return (req) => req.type === match[1] }
return () => false  // 不匹配任何需求
```

- 只支持 `requirement.type === 'xxx'` 格式
- `req.type === 'xxx'` 或 `requirement.type == "xxx"` 直接返回 false
- Skill 永远不被匹配 → 编码阶段走 coding-agent（更慢、更不稳定）

**修复**：放宽正则 + 增加类型名 fallback：
```typescript
const match = expression.match(/(?:requirement|req)\.type\s*={2,3}\s*['"](\w+)['"]/)
if (match) { return (req) => req.type === match[1] }
const typeNames = ['add_field', 'add_page', 'modify_api', 'delete_field', 'delete_page']
for (const name of typeNames) {
  if (expression.includes(name)) { return (req) => req.type === name }
}
return () => false
```

**影响**：Skill 注册制失效

---

### 4.5 orchestrator.ts:162,414 `as any` 类型擦除 🔵 技术债

**问题**：`RequirementMemory` 接口没定义 `findChangesByEntity` 和 `saveChanges`，只能 `as any` 调用。

**修复**：扩展接口定义可选方法：
```typescript
export interface RequirementMemory {
  // ... 现有方法
  saveChanges?(requirementId: string, files: { path: string; action: string }[]): Promise<void>
  findChangesByEntity?(keyword: string): Promise<{ requirementId: string; files: ChangeRecord[] }[]>
}
```

---

### 4.6 db.ts:149 每次写操作同步落盘 🔵 技术债

**问题**：`execute()` 每次都 `writeFileSync` 整个 DB。DELETE 需求连续 5 次调用 = 写 5 次磁盘。

**修复**：添加 `executeBatch()` 支持事务，DELETE 改用批量操作只写一次。

---

### 4.7 移动端侧边栏 `.open` 从未触发 🔵 技术债

**文件**：[App.css:962](frontend/src/App.css#L962)

**问题**：CSS 定义了 `.sidebar.open { left: 0 }`，但 React 代码没有切换 `.open` 的逻辑。移动端用户无法打开侧边栏。

**修复**：Sidebar.tsx 增加汉堡菜单按钮 + `isOpen` state。

---

### 4.8 `plan-approved` 状态命名歧义 🔵 不修

**问题**：orchestrator.ts:233 方案生成后设 `status = 'plan-approved'`，但此时用户还没审批。名字暗示"已审批"，实际是"方案已就绪等待审批"。

**决策**：改名涉及 core + backend + frontend 大量改动，投入产出比低。标记为已知问题，不修。

---

## 五、修复优先级排序

| 编号 | 改进项 | 影响 | 工作量 | 建议 |
|------|--------|------|--------|------|
| P1-1 | orchestrator-runner catch 无条件覆盖 | 高——已完成变失败 | 5 行 | **立即修** |
| 4.1 | clarification-agent 贪婪正则 | 高——JSON 解析失败级联 | 1 行 | **立即修** |
| 4.3 | fallback 垃圾需求 | 中——降级后下游全废 | 5 行 | **尽快修** |
| 4.4 | buildCanHandle 精确匹配 | 中——Skill 永远不触发 | 10 行 | **尽快修** |
| 4.2 | ToolContext 空壳 | 中——路径检查失效 | 20 行 | **建议修** |
| 4.6 | db 事务支持 | 低——性能 | 30 行 | **建议修** |
| 4.5 | as any 类型擦除 | 低——代码质量 | 10 行 | **技术债** |
| 4.7 | 移动端侧边栏 | 低——移动场景少 | 20 行 | **技术债** |
| 4.8 | plan-approved 命名 | 低——不影响功能 | 大量 | **不修** |

---

## 六、整体评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 信任链路 | ⭐⭐⭐⭐ | P0 三件已修，plan/diff 双暂停机制到位 |
| 代码质量 | ⭐⭐⭐ | 类型擦除、空壳注入、正则细节需打磨 |
| 错误处理 | ⭐⭐⭐⭐ | userMessage 体系完整，StatusFlow 展示合理 |
| UX 体验 | ⭐⭐⭐ | 核心流程顺畅，移动端和降级路径有欠缺 |
| 数据层 | ⭐⭐⭐ | 事务缺失、同步落盘，但 MVP 单用户可接受 |
| 竞品差距 | ⭐⭐⭐ | Diff 预览已补齐，缺少版本历史和部分审批 |
