# 分级告警兜底机制 v1.0 实现文档

> 彻底解决"前端显示全部完成，实则功能残缺"的致命静默失败问题

---

## 一、问题背景

### 旧机制的致命缺陷
原来的兜底逻辑是"只要LLM漏返回文件，就静默读取原始内容填充进去"，导致：
- PM 以为需求100%完成了
- 实际上核心变更文件完全没有被修改
- 测试全绿（因为根本没改到代码）
- 没有任何告警提示，属于比直接失败更可怕的静默失败

---

## 二、新机制的三级分级架构

| 等级 | 类型 | 触发条件 | 处理策略 |
|------|------|----------|----------|
| **Level 0** | 完全正常 | LLM 100% 返回了该文件的修改内容 | 直接写入新内容 |
| **Level 1** | 轻微告警 | 该文件只是参考文档/配置文件，不需要核心修改 | 用原始内容兜底，记录黄色告警，PM 可见 |
| **Level 2** | 严重错误 | 该文件明确要求"新增/修改/重构/删除/实现"核心逻辑 | 绝对不兜底，直接判定本轮编码失败，进入重试 |

---

## 三、核心判定规则

### 3.1 关键文件判定（Level 2）
只要 plan 中该文件的 changeDescription 包含以下任意关键词 → 直接判定为关键文件，LLM 漏返回就绝对不兜底：
```typescript
const CRITICAL_KEYWORDS = [
  '新增', 'add', 'Add', 'ADD',
  '修改', 'update', 'Update', 'UPDATE',
  '重构', 'refactor', 'Refactor',
  '删除', 'delete', 'Delete', 'DELETE',
  '实现', 'implement', 'Implement',
]
```

### 3.2 安全兜底文件判定（Level 1）
满足以下任一条件 → 可以安全兜底，只记录黄色告警：
- 文件扩展名是 .md / .txt / .json / .yaml / .yml
- changeDescription 包含"参考/查看/阅读/refer/read/了解/分析"等关键词

### 3.3 可疑文件保守策略
既不命中关键文件也不命中安全文件 → 保守归为 Level 2，直接失败重试，绝不冒险兜底。

---

## 四、新增的两道强制校验关卡

### 关卡1：关键文件缺失检查
在写入文件之前就执行，任何关键文件缺失直接本轮失败，自动进入下一轮重试，给 LLM 注入错误上下文提示它这次务必返回所有文件。

### 关卡2：Diff 空变更检测
写入文件前自动备份所有原始文件为 `.orig`，写入完成后逐文件对比：
- 如果 originalContent === newContent → 判定为"计划修改但实际完全没变更"
- 直接本轮失败，进入重试
- 彻底杜绝 LLM 返回了文件但内容和原始内容完全一模一样的白忙活情况

---

## 五、新增事件类型

### 1. `file-validation` 事件
```typescript
{
  type: 'file-validation',
  summary: {
    totalPlanFiles: 5,
    fullyGenerated: ['a.js', 'b.js', 'c.js'],
    fallbackOriginal: ['README.md'],
    noChangeDetected: [],
    criticalMissing: [],
  }
}
```
PM 在前端第一眼就能看到完整的文件校验摘要。

### 2. 带 warnings 字段的 executing 事件
黄色告警事件直接推送给前端，UI 上用醒目的黄色提示条展示。

### 3. diff-ready 事件新增 fileValidationSummary 字段
在最终 diff 页面最顶部展示完整的校验摘要，PM 在确认提交前100%清楚哪些文件被兜底了。

---

## 六、状态流转变化

```
coding → 生成文件 → 文件映射校验 → 关键文件缺失检查 → 备份原始文件 → 写入磁盘 → Diff 空变更检查 → testing
```

在 coding 和 testing 之间插入了两道强制校验关卡，任何一道不通过都不会往下走。

---

## 七、重试自愈机制

任何校验失败后，自动进入下一轮编码重试，给 LLM 注入错误上下文：
- "上一轮你遗漏了 Article.js 文件的变更，请这次务必返回"
- "上一轮你返回的 articles.js 文件内容和原始内容完全一样，请这次真正实现变更"

最多重试3轮，3轮都失败才最终标记为 failed。

---

## 八、前端 UI 建议展示

在 diff-ready 页面最顶部新增「⚠️ 变更校验摘要」区块：
```
✅ 正常生成文件：3 个
   - server/models/Article.js
   - server/routes/api/articles.js
   - client/src/components/ArticlePreview.js

⚠️ 保留原始内容文件：1 个
   - README.md (仅参考，未修改)

❌ 空变更文件：0 个
❌ 关键缺失文件：0 个
```

PM 确认提交前必须看到这个区块，知情权100%交还给人类。

---

## 九、编译验证结果

✅ TypeScript 编译完全通过，零错误。
