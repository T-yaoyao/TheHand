# TheHand 执行计划：质量修复与体验升级

> 基于全面代码审查的执行计划，分三个优先级推进。
> 生成日期：2026-05-31

---

## 总览

```
P0 信任修复（预计 1 天）  ──── 修复会让用户不再信任系统的 bug
P1 体验加固（预计 1.5 天） ──── 修复会让用户感到困惑/卡住的问题
P2 竞品差距补齐（预计 3-5 天）─── 补足竞品已有、TheHand 缺失的关键能力
```

| 优先级 | 问题数 | 核心主题 |
|--------|--------|---------|
| P0 | 3 | 信任——系统说"完成"但实际没完成 |
| P1 | 5 | 稳固——长时间使用会出问题的隐患 |
| P2 | 4 | 竞争——与 Cursor/Bolt/Lovable 的差距 |

---

## P0：信任修复（Trust Killers）

> 这三个问题不修，用户用一次就不会再用。

### P0-1：编排器 commit/apply 失败后仍标记 `done`

**文件**: [orchestrator.ts:405-411](core/src/orchestrator/orchestrator.ts#L405-L411)

**现状**:
```typescript
} catch (e: any) {
  yield { type: 'executing', phase: `commit failed: ${e.message}`, progress: 95 }
  // 沙箱 commit 失败，不继续 apply 到源仓库
  requirement.status = 'done'          // ← BUG: 应该是 'failed'
  await requirementMemory.saveRequirement(requirement)
  yield { type: 'completed', requirement }  // ← BUG: 应该是 'failed'
  return
}
```

**修复方案**:
```typescript
} catch (e: any) {
  yield { type: 'executing', phase: `commit failed: ${e.message}`, progress: 95 }
  yield { type: 'failed', requirement, error: `提交失败: ${e.message}` }
  requirement.status = 'failed'
  await requirementMemory.saveRequirement(requirement)
  await requirementMemory.saveLesson({
    id: crypto.randomUUID(),
    projectId,
    phase: 'commit',
    filePath: null,
    errorSummary: `Git commit 失败: ${e.message}`,
    errorDetail: e.stack ?? null,
    fixHint: '检查沙箱 git 配置和文件权限',
    resolved: false,
    createdAt: new Date(),
  })
  return
}
```

**同时修复** [orchestrator.ts:420-422](core/src/orchestrator/orchestrator.ts#L420-L422) `applyToSource` 失败被静默吞掉的问题:
```typescript
} catch (e: any) {
  yield { type: 'executing', phase: `apply failed: ${e.message}`, progress: 98 }
  // 不要继续到 "done"，让用户知道变更没有应用到源仓库
  yield { type: 'failed', requirement, error: `应用到源仓库失败: ${e.message}` }
  requirement.status = 'failed'
  await requirementMemory.saveRequirement(requirement)
  return
}
```

**验收标准**: commit 或 apply 失败时，前端显示红色"失败"状态，而非绿色"完成"。

---

### P0-2：确认/拒绝方案按钮是假的

**文件**: [App.tsx:370-381](frontend/src/App.tsx#L370-L381)

**现状**: 两个按钮都只是 `showToast('success', ...)` ，没有任何后端调用。

**修复方案**:

**后端新增 API** — [orchestrator.ts](backend/src/routes/orchestrator.ts):
```typescript
// POST /api/requirements/:id/approve-plan
router.post('/requirements/:id/approve-plan', async (req, res) => {
  const { id } = req.params
  const requirement = queryOne('SELECT * FROM requirements WHERE id = ?', [id])
  if (!requirement) return res.status(404).json({ error: '需求不存在' })
  if (requirement.status !== 'plan-approved' && requirement.status !== 'clarified') {
    return res.status(400).json({ error: `当前状态 ${requirement.status} 不可审批` })
  }
  // 状态改为 plan-approved，触发编码阶段
  execute(`UPDATE requirements SET status = 'plan-approved', updated_at = datetime('now') WHERE id = ?`, [id])
  // 自动触发编排器继续执行
  runOrchestratorForRequirement(id).catch(e => log.error('[approve-plan]', e))
  res.json({ ok: true })
})

// POST /api/requirements/:id/reject-plan
router.post('/requirements/:id/reject-plan', async (req, res) => {
  const { id } = req.params
  const { reason } = req.body
  execute(`UPDATE requirements SET status = 'plan-rejected', updated_at = datetime('now') WHERE id = ?`, [id])
  // 记录驳回原因
  if (reason) {
    execute(`INSERT INTO conversations (id, requirement_id, role, content, round) VALUES (?, ?, 'pm', ?, 0)`,
      [randomUUID(), id, `[驳回方案] ${reason}`])
  }
  res.json({ ok: true })
})
```

**前端修复** — [App.tsx:370-381](frontend/src/App.tsx#L370-L381):
```typescript
{selected.status === 'plan-approved' && (
  <div className="plan-actions">
    <button type="button" className="btn-primary" onClick={async () => {
      try {
        await api.approvePlan(selected.id)
        showToast('success', '方案已确认，开始编码')
        refreshList()
      } catch (e) {
        showToast('error', e instanceof Error ? e.message : '确认失败')
      }
    }}>
      确认方案
    </button>
    <button type="button" className="btn-secondary" onClick={async () => {
      const reason = prompt('请输入驳回原因（可选）:')
      try {
        await api.rejectPlan(selected.id, reason ?? undefined)
        showToast('success', '方案已驳回')
        refreshList()
      } catch (e) {
        showToast('error', e instanceof Error ? e.message : '驳回失败')
      }
    }}>
      驳回方案
    </button>
  </div>
)}
```

**前端 API 封装** — [api.ts](frontend/src/services/api.ts):
```typescript
approvePlan: (id: string) =>
  fetch(`${BASE_URL}/requirements/${id}/approve-plan`, { method: 'POST' }).then(handleResponse),
rejectPlan: (id: string, reason?: string) =>
  fetch(`${BASE_URL}/requirements/${id}/reject-plan`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ reason }),
  }).then(handleResponse),
```

**验收标准**: 点击"确认方案"后流水线继续执行编码阶段；点击"驳回方案"后状态变为 `plan-rejected`。

---

### P0-3：DELETE 需求不清理 `lessons` 和 `change_history`

**文件**: [requirements.ts:162-171](backend/src/routes/requirements.ts#L162-L171)

**现状**: 只删了 `conversations` 和 `executions`，留下了 `lessons` 和 `change_history` 孤儿数据。

**修复方案**:
```typescript
requirementsRouter.delete('/:id', (req: Request, res: Response) => {
  const id = req.params.id
  try {
    // 按依赖顺序删除（外键约束）
    execute('DELETE FROM conversations WHERE requirement_id = ?', [id])
    execute('DELETE FROM executions WHERE requirement_id = ?', [id])
    execute('DELETE FROM change_history WHERE requirement_id = ?', [id])
    // lessons 没有 requirement_id 外键，但 project_id 关联的数据也应清理
    // 如果 lessons 有 requirement_id 字段则一并删除
    execute('DELETE FROM requirements WHERE id = ?', [id])
    res.json({ ok: true })
  } catch (e: any) {
    res.status(500).json({ error: `删除失败: ${e.message}` })
  }
})
```

**可选增强**: 把多次 `execute` 包在事务里（sql.js 支持 `db.exec('BEGIN...COMMIT')`），但如果只修这一个问题，当前方式足够。

**验收标准**: 删除需求后，查询 `lessons` 和 `change_history` 表确认无孤儿数据。

---

## P1：体验加固（Experience Hardening）

> 不会立即让信任崩塌，但长时间使用或异常路径会出问题。

### P1-1：`orchestrator-runner.ts` catch 块覆盖已完成状态

**文件**: [orchestrator-runner.ts:132-135](backend/src/orchestrator-runner.ts#L132-L135)

**现状**: 顶层 catch 无条件写入 `status = 'failed'`，即使编排器内部已经正确标记了 `done`。

**修复方案**:
```typescript
} catch (e: unknown) {
  const message = e instanceof Error ? e.message : String(e)
  // 仅在状态不是终态时才覆盖为 failed
  const current = queryOne('SELECT status FROM requirements WHERE id = ?', [requirementId]) as { status: string } | undefined
  if (current && current.status !== 'done' && current.status !== 'failed') {
    execute(`UPDATE requirements SET status = 'failed', updated_at = datetime('now') WHERE id = ?`, [requirementId])
  }
  pushEvent(requirementId, {
    type: 'failed',
    requirement: { id: requirementId, status: current?.status === 'done' ? 'done' : 'failed' },
    error: message,
  })
  log.error(`[orchestrator] 异常 id=${requirementId.slice(0, 8)}…`, message)
}
```

**验收标准**: 正常完成的需求即使 post-completion 出现异常（如清理沙箱失败），状态仍为 `done`。

---

### P1-2：`db-requirement-memory.ts` JSON.parse 无 try/catch

**文件**: [db-requirement-memory.ts:11-13](backend/src/db-requirement-memory.ts#L11-L13)（假设位置，需确认）

**现状**: `JSON.parse(requirement.structured_requirement)` 如果数据库中存储了损坏的 JSON，直接抛异常。

**修复方案**: 在所有 `JSON.parse` 调用处包裹 try/catch:
```typescript
function safeJsonParse(str: string | null): any {
  if (!str) return null
  try {
    return JSON.parse(str)
  } catch {
    log.warn('[db] JSON parse 失败，返回 null:', str.slice(0, 100))
    return null
  }
}
```

**验收标准**: 即使数据库中有损坏的 JSON 字段，API 仍正常返回（降级为 null）。

---

### P1-3：LLMClient `tokenHistory` 内存泄漏

**文件**: [llm-client.ts:61](core/src/llm/llm-client.ts#L61)

**现状**: `tokenHistory` 数组只增不减，长时间运行内存持续增长。

**修复方案**:
```typescript
private tokenHistory: TokenRecord[] = []
private static MAX_TOKEN_HISTORY = 1000

// 在 push 时检查
this.tokenHistory.push({ ... })
if (this.tokenHistory.length > LLMClient.MAX_TOKEN_HISTORY) {
  this.tokenHistory = this.tokenHistory.slice(-500)  // 保留最近一半
}
```

**验收标准**: 长时间运行后 `tokenHistory` 不超过 1000 条。

---

### P1-4：前端 SSE 事件数组无上限

**文件**: [useSSE.ts:55](frontend/src/hooks/useSSE.ts#L55)

**现状**: `events` 数组持续增长，长时间使用导致页面卡顿。

**修复方案**:
```typescript
const MAX_EVENTS = 500

setEvents(prev => {
  const next = [...prev, parsed]
  return next.length > MAX_EVENTS ? next.slice(-MAX_EVENTS / 2) : next
})
```

**验收标准**: 长时间运行后事件数组不超过 500 条。

---

### P1-5：前端 toast setTimeout 未清理

**文件**: [App.tsx:39-41](frontend/src/App.tsx#L39-L41)

**现状**: `setTimeout` 返回值未保存，组件卸载后仍执行 `setToast(null)`，React 警告。

**修复方案**:
```typescript
const toastTimer = useRef<ReturnType<typeof setTimeout>>()

const showToast = useCallback((type: 'error' | 'success', msg: string) => {
  if (toastTimer.current) clearTimeout(toastTimer.current)
  setToast({ type, msg })
  toastTimer.current = setTimeout(() => setToast(null), 4000)
}, [])

// 在 useEffect cleanup 中清理
useEffect(() => {
  return () => { if (toastTimer.current) clearTimeout(toastTimer.current) }
}, [])
```

**验收标准**: 组件卸载时无 React 内存泄漏警告。

---

## P2：竞品差距补齐（Competitive Gaps）

> Cursor / Bolt.new / Lovable 已有的核心能力，TheHand 需要补足。

### P2-1：Diff 预览（对标 Cursor）

**目标**: 代码生成后、commit 前，PM 能看到变更内容。

**涉及文件**:
- 新增 `frontend/src/components/DiffViewer.tsx`（已有骨架，需完善）
- 修改 [orchestrator.ts](core/src/orchestrator/orchestrator.ts) 在 commit 前暂停
- 新增后端 API `GET /api/requirements/:id/diff`

**实现要点**:
1. 编码阶段完成后、commit 前，发出 `diff-ready` 事件暂停流程
2. 前端展示 diff（使用 `react-diff-viewer` 或简单的 unified diff）
3. PM 点击"确认提交"后才继续 commit
4. PM 点击"撤回变更"则 rollback

**验收标准**: 生成代码后，前端展示 diff，PM 确认后才 commit。

---

### P2-2：错误信息用户友好化

**涉及文件**:
- [orchestrator.ts](core/src/orchestrator/orchestrator.ts) — 所有 `yield { type: 'failed', ... }`
- [orchestrator-runner.ts](backend/src/orchestrator-runner.ts) — 错误事件格式
- [StatusFlow.tsx](frontend/src/components/StatusFlow.tsx) — 错误展示
- [App.tsx](frontend/src/App.tsx) — 错误操作指引

**实现要点**:
1. 区分"用户可理解"和"开发者调试"信息
2. 失败事件增加 `userMessage` 和 `debugInfo` 两个字段
3. 前端优先展示 `userMessage`，点击"详情"展开 `debugInfo`
4. 增加"重试"按钮（对可重试的错误）

**验收标准**: LLM 调用失败、lint 失败、commit 失败等场景，前端展示用户友好的错误信息和操作指引。

---

### P2-3：`reverted` 状态纳入 VALID_STATUSES

**文件**: [requirements.ts:16](backend/src/routes/requirements.ts#L16)

**现状**: `reverted` 在 `core/src/types.ts` 中定义，但不在后端 `VALID_STATUSES` 中，导致 PATCH 更新为 `reverted` 会被拒绝。

**修复方案**:
```typescript
const VALID_STATUSES = ['idle', 'clarifying', 'clarified', 'waiting-for-pm', 'planning', 'plan-approved', 'plan-rejected', 'coding', 'testing', 'done', 'failed', 'reverted']
```

**验收标准**: revert 操作成功后需求状态正确变为 `reverted`。

---

### P2-4：前端 `handleResponse` 兼容 204

**文件**: [api.ts:28-34](frontend/src/services/api.ts#L28-L34)

**现状**: `handleResponse` 假设所有响应都是 JSON。DELETE 可能返回 204 No Content，导致 `response.json()` 抛异常。

**修复方案**:
```typescript
async function handleResponse<T>(response: Response): Promise<T> {
  if (!response.ok) {
    const error = await response.text()
    throw new Error(error || `请求失败 (${response.status})`)
  }
  if (response.status === 204) return {} as T
  return response.json()
}
```

**验收标准**: DELETE 操作不报 JSON 解析错误。

---

## 执行顺序建议

```
Day 1 上午:  P0-1 (commit/apply 失败标记) → 最关键的信任问题
Day 1 下午:  P0-2 (确认/驳回方案按钮) → 涉及前后端 + API
Day 1 晚上:  P0-3 (DELETE 清理) + P1-1 (catch 覆盖) + P1-2 (JSON.parse) → 小修复批量处理

Day 2 上午:  P1-3 (tokenHistory) + P1-4 (events 数组) + P1-5 (toast timer) → 内存问题
Day 2 下午:  P2-3 (reverted 状态) + P2-4 (204 处理) → 快速修复

Day 3-5:    P2-1 (Diff 预览) + P2-2 (错误友好化) → 功能增强，需要更多设计和测试
```

## 验证清单

每个修复完成后，按以下清单验证：

- [ ] P0-1: commit 失败时前端显示红色失败状态
- [ ] P0-2: 点击"确认方案"触发编码阶段，点击"驳回方案"状态变为 plan-rejected
- [ ] P0-3: 删除需求后查询 lessons/change_history 无孤儿数据
- [ ] P1-1: 沙箱清理异常不影响需求最终状态
- [ ] P1-2: 数据库有损坏 JSON 时 API 正常返回
- [ ] P1-3: tokenHistory 数组不超过 1000 条
- [ ] P1-4: SSE 事件数组不超过 500 条
- [ ] P1-5: 组件卸载无 React 警告
- [ ] P2-1: 生成代码后展示 diff，确认后 commit
- [ ] P2-2: 失败场景展示用户友好错误信息
- [ ] P2-3: revert 后状态正确变为 reverted
- [ ] P2-4: DELETE 操作不报 JSON 解析错误
