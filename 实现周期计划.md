# SuperMan 实现周期计划

> 2 人团队，2.5 周（12.5 个工作日），从零到端到端交付。

---

## 总览

```
Week 1 (D1-D5)    ──── P0 核心链路：AI 骨架 + 工程骨架
Week 2 (D6-D10)   ──── P0 全链路联调 + 验收，冲刺 P1
Week 3 (D11-D12.5) ──── P1 加分项 + 修复 + 答辩准备
```

### 里程碑

| 里程碑 | 时间 | 验收标准 |
|---|---|---|
| M1：CLI 跑通 | D5 末 | CLI 输入需求 → 澄清 → 方案 → 生成代码文件，无需前端 |
| M2：全链路联调 | D8 末 | 从 Web 页面输入需求到 PR 创建，端到端跑通 |
| M3：P0 验收 | D10 末 | 三级验证通过（Lint + 单测 + 集成测试），PR 成功创建 |
| M4：P1 交付 | D12 末 | 至少 3 个加分项落地 |
| M5：最终交付 | D12.5 | 演练 + 答辩材料 |

---

## 角色分工

| 角色 | 职责定位 | 核心交付 |
|---|---|---|
| **A（AI 核心）** | Agent 体系 + Skill + Prompt + LLM 调用 | 四个 Agent 能跑通，生成的代码能通过验证 |
| **B（工程支撑）** | 后端 API + 状态机 + Git 操作 + 前端 | Web 界面能用，全链路串联，PR 能创建 |

### 协作规则

- **接口先行**：每个阶段开始前，A/B 先对齐接口定义（输入输出 JSON Schema），再各自开发
- **每日同步**：每天末尾 15 分钟，互相演示当日进展，暴露阻塞
- **联调优先**：宁可各自功能少一点，也要尽早联调。联调暴露的问题比各自开发多 10 倍

---

## P0 MVP（D1-D10）

### 目标

端到端跑通一条需求：**"给 Article 模型新增 readTime 字段"**

验收标准：
1. PM 在 Web 页面输入需求
2. 系统澄清 → 方案 → 生成代码 → 验证 → 创建 PR
3. Lint + 单测 + 集成测试全部通过
4. PR 包含正确的代码变更

---

### D1：项目初始化 + 模型验证

**A 和 B 共同参与**

| 任务 | 负责 | 产出 |
|---|---|---|
| clone Conduit 仓库到 sandbox-repo，跑通 lint/test/dev | B | sandbox-repo 可正常运行 |
| 搭建 monorepo 骨架（superman/ 目录结构） | B | 项目骨架 + package.json + tsconfig |
| 手动验证 doubao-seed-2.0 lite 模型能力 | A | 验证报告：哪些环节能用，哪些需要拆分 |
| 写第一个 prompt：模糊需求 → 结构化 JSON | A | prompts/clarification/v1.md |
| 写第二个 prompt：结构化需求 + 文件上下文 → 生成代码 | A | prompts/coding/v1.md |
| 约定核心接口：AgentResult、SkillOutput、ToolResult | A + B | types.ts（接口定义文件） |

**D1 结束标准**：
- sandbox-repo 能跑 `npm test` 和 `npm run lint`
- 模型验证报告产出，明确 doubao 在各环节的表现
- 核心接口定义文件确认

---

### D2：LLM 调用层 + 澄清 Agent

| 任务 | 负责 | 产出 |
|---|---|---|
| 实现 LLMClient（doubao API 封装） | A | core/llm/llm-client.ts |
| 实现 PromptManager（模板加载 + 变量替换） | A | core/llm/prompt-manager.ts |
| 实现澄清 Agent | A | core/agents/clarification-agent.ts |
| CLI 测试：输入模糊需求 → 输出结构化 JSON | A | CLI 工具可手动测试 |
| 搭建 Express 后端骨架 + SQLite | B | backend/ 可启动，health check 通过 |
| 实现需求 CRUD API（POST/GET /api/requirements） | B | API 可用 Postman 测试 |
| 搭建 React + Vite 前端骨架 | B | frontend/ 可启动，空白页面 |

**D2 结束标准**：
- A：CLI 输入"给文章加阅读时长"，澄清 Agent 能输出结构化 JSON（手动测试）
- B：后端能创建和查询需求，前端能启动

---

### D3：方案 Agent + 编码 Agent

| 任务 | 负责 | 产出 |
|---|---|---|
| 实现方案 Agent（结构化需求 → 文件修改方案） | A | core/agents/plan-agent.ts |
| 实现编码 Agent（方案 → 生成完整文件内容） | A | core/agents/coding-agent.ts |
| 实现 add-field Skill | A | projects/conduit/skills/add-field/SKILL.md |
| 实现 file-read 工具 | A | core/tools/file-read-tool.ts |
| 实现 file-write 工具（含路径穿越检查） | A | core/tools/file-write-tool.ts |
| 实现 SSE 推送（后端 → 前端实时状态） | B | backend SSE endpoint |
| 前端对话面板骨架 | B | ChatPanel.tsx（能发消息、显示回复） |

**D3 结束标准**：
- A：CLI 跑通"澄清 → 方案 → 生成代码文件"三步，代码文件写入 sandbox-repo
- B：前端能通过 SSE 实时显示后端推送的状态

---

### D4：测试 Agent + Git 操作

| 任务 | 负责 | 产出 |
|---|---|---|
| 实现测试 Agent（lint + 单测执行 + 结果解析） | A | core/agents/test-agent.ts |
| 实现 shell 工具（执行 lint/test 命令） | A | core/tools/shell-tool.ts |
| Agent 执行器（AgentRunner，对标 runAgent） | A | core/agents/agent-runner.ts |
| 实现 RepoManager（clean check/rollback/commit） | B | core/git-ops/repo-manager.ts |
| 实现 Diff 检查器 | B | core/git-ops/diff-checker.ts |
| 实现 PR 创建器（gh CLI 封装） | B | core/git-ops/pr-creator.ts |
| 前端状态流展示组件 | B | StatusFlow.tsx |

**D4 结束标准**：
- A：生成的代码能通过 lint 和单测（CLI 验证）
- B：Git 操作封装完成，能 clean check → commit → 创建 PR（手动测试）

---

### D5：CLI 全链路联调（M1 里程碑）

| 任务 | 负责 | 产出 |
|---|---|---|
| 实现 Orchestrator 状态机 | A + B | core/orchestrator/orchestrator.ts |
| CLI 全链路联调：输入需求 → 澄清 → 方案 → 代码 → 测试 → 提交 | A + B | CLI 端到端跑通 |
| 修复联调中发现的问题 | A + B | bug fixes |
| 记录联调过程，明确 API 对接点 | B | 接口文档更新 |

**D5 结束标准（M1）**：
- CLI 输入"给 Article 加 readTime"，自动完成澄清 → 方案 → 代码生成 → lint 通过 → 测试通过 → git commit
- 明确哪些环节需要优化（prompt 调整、错误处理等）

---

### D6：前后端对接

| 任务 | 负责 | 产出 |
|---|---|---|
| Agent 层对接后端 API（A 的 Agent 被 B 的 API 调用） | A | API 能触发 Agent 执行 |
| 前端对接 SSE（需求创建 → 实时状态推送） | B | 前端能实时看到状态变化 |
| 前端对接 PM 回复 API（澄清阶段的交互） | B | PM 能在前端回复澄清问题 |
| Prompt 优化（根据 D5 联调结果调整） | A | prompts/ 更新 |
| 前端方案审批组件 | B | PlanApproval.tsx |

**D6 结束标准**：
- 从 Web 页面输入需求，能看到澄清追问，PM 回复后能看到方案

---

### D7：方案审批 + 执行流程

| 任务 | 负责 | 产出 |
|---|---|---|
| 方案审批逻辑（确认/修改/终止） | B | 审批 API + 前端交互 |
| 编码执行流程串联（审批通过 → 生成代码 → 写入） | A | 编码阶段完整跑通 |
| 测试执行流程串联（写入 → lint → 测试） | A | 测试阶段完整跑通 |
| 前端执行进度展示 | B | ExecutionProgress.tsx |
| 前端 PR 预览组件 | B | PRPreview.tsx |

**D7 结束标准**：
- 从 Web 页面：输入需求 → 澄清 → 看到方案 → 点击确认 → 代码生成中 → 测试通过 → PR 已创建

---

### D8：全链路联调（M2 里程碑）

| 任务 | 负责 | 产出 |
|---|---|---|
| 全链路联调（Web → 后端 → Agent → Git → PR） | A + B | 端到端跑通 |
| 错误处理完善（各阶段的异常捕获和友好提示） | A + B | 错误处理 |
| Diff 查看组件 | B | DiffViewer.tsx |
| 修复联调问题 | A + B | bug fixes |

**D8 结束标准（M2）**：
- Web 页面完整跑通"给 Article 加 readTime"全流程
- PR 能在 GitHub 上查看
- 至少测试 3 条不同的需求，确认稳定性

---

### D9-D10：P0 验收 + 修复

| 任务 | 负责 | 产出 |
|---|---|---|
| 三级验证全量通过（Lint + 单测 + 集成测试） | A | 验证报告 |
| 测试多条 L1 练手题（阅读量、字数统计等） | A | 测试报告 |
| 测试 L2 练手题（封面图、最后编辑时间） | A | 测试报告 |
| 前端 UI 打磨（状态展示、错误提示、交互优化） | B | UI 完善 |
| 回滚机制验证（故意触发失败，确认自动回滚） | B | 回滚测试 |
| README 编写（项目说明、部署方式、技术架构） | B | README.md |

**D10 结束标准（M3）**：
- 至少 3 条 L1 需求端到端跑通
- 至少 1 条 L2 需求端到端跑通
- 三级验证全部通过
- README 可读

---

## P1 加分项（D11-D12）

根据剩余时间和实现难度，按优先级选择性实现：

### P1 优先级排序（按性价比）

| 排名 | 加分项 | 预估工时 | 负责 | 理由 |
|---|---|---|---|---|
| 1 | **P1-1 Skill 注册制** | 3h | A | MVP 已有 add-field Skill，抽象为注册制改动不大 |
| 2 | **P1-2 澄清深度** | 4h | A | 在现有澄清 Agent 基础上增强歧义识别 |
| 3 | **P1-6 可观测性** | 3h | B | Token/延迟/成本记录已在 LLMClient 中有埋点，只缺前端面板 |
| 4 | **P1-3 断点重放** | 4h | B | Orchestrator 状态机增加暂停/恢复逻辑 |
| 5 | **P1-4 跨栈一致性** | 6h | A | 难度高，需要编码 Agent 理解前后端关联 |
| 6 | **P1-5 业务上下文反哺** | 6h | A | 需要向量检索或相似度匹配，工程量大 |

### D11：P1 第一批

| 任务 | 负责 | 产出 |
|---|---|---|
| P1-1：Skill 注册制抽象（自动发现、canHandle 匹配） | A | SkillRegistry 完善 |
| P1-2：澄清深度增强（歧义识别 prompt 优化 + 多轮追问策略） | A | 澄清 Agent 升级 |
| P1-6：可观测性面板（Token 用量、延迟、成本展示） | B | 监控面板页面 |
| 测试更多 L1/L2 需求，修复边缘问题 | A + B | 测试报告 |

### D12：P1 第二批

| 任务 | 负责 | 产出 |
|---|---|---|
| P1-3：断点重放（流程暂停/修改/重放下游） | B | 状态机升级 |
| P1-4 或 P1-5 选择一个实现（如果时间允许） | A | 加分项 |
| L3 挑战题测试（模糊需求澄清能力） | A | 测试报告 |

---

## P2 优化（D12.5，如有余力）

| 任务 | 说明 |
|---|---|
| 性能优化 | LLM 调用并发、缓存项目上下文、减少重复 prompt |
| Prompt 精调 | 根据测试结果持续优化各 Agent 的 prompt |
| 演示准备 | 录制演示视频、准备答辩材料、梳理亮点 |
| 极端场景测试 | 测试失败、网络中断、模型输出异常等边界情况 |

---

## 每日节奏

| 时间 | 活动 |
|---|---|
| 09:00 | 15 分钟站会：昨日进展、今日计划、阻塞项 |
| 全天 | 各自开发，阻塞时立即沟通 |
| 18:00 | 15 分钟同步：演示当日成果，确认次日优先级 |
| D5/D8/D10 | 正式里程碑验收，对齐下一步计划 |

---

## 风险预案

| 风险 | 触发条件 | 应对 |
|---|---|---|
| 模型生成代码质量差 | lint/测试通过率 < 50% | 拆分 prompt 为更小子任务；必要时在编码环节升级模型（自费） |
| 澄清 Agent 不稳定 | 结构化 JSON 格式经常出错 | 加 JSON schema 校验 + 自动重试；降级为模板填充 |
| 联调耗时超预期 | D8 未跑通全链路 | 砍前端交互，先保证 CLI 全链路；Web 界面降级为只读展示 |
| 测试 Agent 跑不通 | 集成测试环境搭建困难 | 集成测试降级为手动验证，优先保 lint + 单测 |
| 时间不够做 P1 | D11 仍有 P0 问题 | 放弃 P1，集中精力打磨 P0 演示效果 |

---

## 工时分配汇总

| 模块 | A 工时 | B 工时 | 合计 |
|---|---|---|---|
| 模型验证 + Prompt 编写 | 1.5d | 0d | 1.5d |
| LLM 调用层 | 1d | 0d | 1d |
| 四个 Agent 实现 | 3d | 0d | 3d |
| Skill 体系 | 1d | 0d | 1d |
| 工具管线（file/shell/git） | 1d | 1d | 2d |
| Orchestrator 状态机 | 1d | 1d | 2d |
| 后端 API + SSE | 0d | 2d | 2d |
| 前端页面 | 0d | 3d | 3d |
| Git 操作 + PR 创建 | 0d | 1d | 1d |
| 联调 + 修复 | 1.5d | 1.5d | 3d |
| P1 加分项 | 1.5d | 1d | 2.5d |
| **合计** | **12.5d** | **10.5d** | **23d** |

A 的工时较满（12.5d），B 有 2d 余量用于支援 A 或处理突发问题。

---

## 接口对齐清单

以下接口在开发前必须 A/B 对齐，避免联调时返工：

| 接口 | A 提供 | B 提供 | 对齐时间 |
|---|---|---|---|
| Agent 执行入口 | `AgentRunner.run(agent, context) → AgentResult` | 后端 API 如何调用 AgentRunner | D1 |
| SSE 事件格式 | Agent 执行中的状态事件结构 | SSE 推送协议 | D2 |
| 结构化需求 JSON | 澄清 Agent 的输出格式 | 后端存储格式 | D1 |
| 方案 JSON | 方案 Agent 的输出格式 | 前端渲染格式 | D3 |
| 代码生成结果 | 编码 Agent 的输出格式 | file-write 工具的输入格式 | D3 |
| 测试结果 | 测试 Agent 的输出格式 | 前端展示格式 | D4 |
| Git 操作接口 | — | RepoManager 的方法签名 | D4 |
